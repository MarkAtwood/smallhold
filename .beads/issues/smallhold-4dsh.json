{
  "id": "smallhold-4dsh",
  "title": "GET/POST/DELETE /api/v1/bookmarks — local per-persona bookmark table",
  "description": "## What\nImplement local-only bookmarks. Bookmarks are stored locally and never federated. POST /api/v1/statuses/:id/bookmark adds a bookmark. POST /api/v1/statuses/:id/unbookmark removes it. GET /api/v1/bookmarks returns a paginated list of bookmarked statuses as Status objects. Requires a bookmarks table keyed on (account_id, post_id or remote_post_id).\n\n## Why\nBookmarks are used heavily in Ivory and Elk as a reading list. Without them the bookmark button appears to do nothing. Part of sub-epic 8-D.\n\n## Agent team pattern\n- Spawn a Research subagent to: curl mastodon.social GET /api/v1/bookmarks to document the Status array response shape and Link header pagination format; check Status object for the bookmarked boolean field and document when it is true; verify POST /api/v1/statuses/:id/bookmark returns the full updated Status object (not 204).\n- Spawn an Implementation subagent to: add bookmarks migration (id snowflake PRIMARY KEY, account_id INTEGER, post_id INTEGER NULLABLE, remote_post_id INTEGER NULLABLE, created_at INTEGER, UNIQUE(account_id, post_id), UNIQUE(account_id, remote_post_id), CHECK(post_id IS NOT NULL OR remote_post_id IS NOT NULL)); implement POST /api/v1/statuses/:id/bookmark: resolve status ID to local or remote post; INSERT OR IGNORE into bookmarks; return full Status object with bookmarked=true; implement POST /api/v1/statuses/:id/unbookmark: DELETE from bookmarks; return full Status object with bookmarked=false; implement GET /api/v1/bookmarks: paginated query with max_id/min_id/since_id/limit support using snowflake ordering on bookmarks.id; JOIN to posts/remote_posts; serialize each as Status; set Link header.\n- Spawn a Test subagent to: write integration tests with in-memory SQLite; create local post; bookmark it; assert GET /api/v1/bookmarks returns it with bookmarked=true; unbookmark it; assert it does not appear in bookmarks list; assert Status.bookmarked=false in other endpoints after unbookmark; assert bookmark is per-account (account A's bookmark not visible to account B).\n\n## Test discipline\n- Independent oracle: mastodon.social GET /api/v1/bookmarks live fixture for Status array and Link header format; mastodon.social POST /api/v1/statuses/:id/bookmark response shape (returns full Status, not 204).\n- Test approach: integration test with in-memory SQLite; create two local personas; bookmark post with persona A; assert it appears in persona A's bookmarks; assert it does NOT appear in persona B's bookmarks (isolation check); pagination test: create 5 bookmarks; query with limit=2; follow Link header next; verify non-overlapping pages.\n\n## Defensive programming checklist\n- Bookmark is per-persona not per-server (always filter by authed account_id)\n- Double-bookmark is idempotent (INSERT OR IGNORE); return 200 not 422\n- Double-unbookmark is idempotent; return 200 with Status.bookmarked=false\n- Bookmarking a nonexistent status returns 404 (not panic)\n- No .unwrap() in handler paths\n- Pagination uses snowflake IDs on bookmarks.id, not OFFSET",
  "status": "closed",
  "priority": 3,
  "issue_type": "task",
  "owner": "mark@wolfssl.com",
  "created_at": "2026-04-23T00:54:40Z",
  "created_by": "Mark Atwood",
  "updated_at": "2026-07-18T17:21:29.961825768Z",
  "closed_at": "2026-07-18T17:21:29.961825768Z",
  "close_reason": "Bookmarks already in posting.rs",
  "metadata": {},
  "dependencies": [
    {
      "issue_id": "",
      "depends_on_id": "smallhold-664l",
      "type": "relates-to",
      "created_at": "2026-04-22T17:54:42Z",
      "created_by": "Mark Atwood",
      "metadata": "{}"
    }
  ]
}
