{
  "compaction_level": 0,
  "created_at": "2026-04-23T04:17:19Z",
  "created_by": "Mark Atwood",
  "description": "## What\nAccept `spoiler_text` and `sensitive` parameters in POST /api/v1/statuses; validate spoiler_text length <= max_post_chars; pass both through to post creation logic.\n\n## Why\nWithout accepting these params, clients cannot post content warnings — a core Mastodon feature used for sensitive or spoiler content.\n\n## Agent team pattern\n- Spawn a Research subagent to: review the Mastodon POST /api/v1/statuses API docs and curl mastodon.social to confirm accepted params, validation errors returned for oversized spoiler_text, and how sensitive interacts with media attachments\n- Spawn an Implementation subagent to: update the POST /api/v1/statuses handler to parse spoiler_text and sensitive from request body, validate spoiler_text length, and persist both to the posts table\n- Spawn a Test subagent to: write tests covering (a) post with valid spoiler_text round-trips correctly, (b) spoiler_text exceeding max_post_chars returns 422, (c) sensitive=true is stored and returned\n\n## Test discipline\n- Independent oracle: mastodon.social /api/v1/statuses POST behavior — oversized spoiler_text returns 422 with error field\n- Test approach: parameterized tests with boundary values for spoiler_text length; assert HTTP status codes match Mastodon reference behavior\n\n## Defensive programming checklist\n- No `.unwrap()` in handler paths\n- Treat spoiler_text as untrusted input — HTML-escape on read (serialization), store raw in DB\n- Validate spoiler_text byte length against same max_post_chars limit as post content\n- Return 422 Unprocessable Entity for validation failures, matching Mastodon behavior",
  "ephemeral": false,
  "id": "smallhold-8ud4",
  "is_template": false,
  "issue_type": "task",
  "metadata": {},
  "no_history": false,
  "owner": "mark@wolfssl.com",
  "pinned": false,
  "priority": 2,
  "status": "open",
  "title": "Accept spoiler_text and sensitive params in POST /api/v1/statuses",
  "updated_at": "2026-04-23T04:17:19Z",
  "dependencies": [
    {
      "created_at": "2026-04-22T21:17:22Z",
      "created_by": "Mark Atwood",
      "depends_on_id": "smallhold-m6xg",
      "metadata": "{}",
      "type": "relates-to"
    }
  ],
  "timeout": 0
}
